<!-- This partial requires the locals: notifications -->

<% notifications.each do |notification| %>
  <%= render partial: "notifly/templates/#{notification.template}", locals: { notification: notification } %>
<% end %>